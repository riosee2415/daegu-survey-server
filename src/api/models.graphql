type User {
  id: ID!
  round: Int!
  username: String!
  mobile: Int!
  isExpert: Boolean!
  createdAt: String!
  updatedAt: String!
}

type AcceptRecord {
  id: ID!
  date: String!
}

type Question {
  id: ID!
  round: Int!
  quetionTitle: String!
  sort: Int!
  createdAt: String!
  updatedAt: String!
}
