type Query {
  getQuestion(round: Int!): [Question!]!
}

type Mutation {
  deleteQuestion(id: String!): Boolean!
  modifyQuestionSort(id: String!, sort: Int!): Boolean!
  createQuestion(round: Int!, quetionTitle: String!, sort: Int!): Boolean!
}
